# KHÔNG SỬ DỤNG - Dùng Azure AKS thay thế
# Xem file setup-aks.bat để tạo cluster trên Azure

# Nếu muốn dùng Kind (local), uncomment bên dưới:
# kind: Cluster
# apiVersion: kind.x-k8s.io/v1alpha4
# name: crypto-cluster
# nodes:
#   - role: control-plane
#   - role: worker
#   - role: worker
